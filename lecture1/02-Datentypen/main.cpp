#include <iostream>

int main() {
  // Boolean Werte
  // bool istok;
  // istok = true;

  // std::cout << "der Wert von istok ist " << istok << std::endl;
  // std::cout << "die Groesse (in bytes) von istok ist " << sizeof(istok) << std::endl;

  // if(istok) {
  //   std::cout << "istok ist wahr" <<std::endl;
  // } else {
  //   std::cout << "istok ist falsch" <<std::endl;
  // }
  
  // Caharacterwerte
  // char zeichen;
  // zeichen = 'A';

  // std::cout << "der Wert von zeichen ist:" << zeichen <<std::endl;
  // std::cout << "Die Groesse (in Bytes) von zewichen ist:" << sizeof(zeichen) << std::endl;

  // if(zeichen) {
  //   std::cout << "zeichen ist wahr" << std::endl;
  // } else {
  //   std::cout << "zeichen ist falsch" << std::endl;
  // }
  
  // Beispiele für Ganzzahlen

  // int n;
  // n = -100;
  // std::cout << "Der Wert von n ist: " << n <<std:endl;
  // std::cout << "Die Groesse (in bytes) von n ist:" << sizeof(n) << std::endl;
  
  // Strings
  // std::string str;
  // str = "Hallo Welt";
  // std::cout << "Der Wert von str ist: " << str << std::endl;
  // std::cout << "Die Groesse (in bytes) von str ist: " << sizeof(str) << std::endl;

  // int k = 12;
  // std::string str = "Krickel";

  // auto v1 = k;

  // std::cout << "Typ von v1 ist: " << typeid(v1).name() << std::endl;

  // auto v2 = "Meier";

  // std::cout << "Type von v2 ist: " << typeid(v2).name() << std::endl;
}
